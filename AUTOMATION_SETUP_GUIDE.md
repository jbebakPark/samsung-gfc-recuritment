# 🤖 완전 자동화 워크플로우 - 사전 준비 가이드

**프로젝트**: 삼성생명 GFC 채용 사이트  
**작성일**: 2026년 2월 12일  
**버전**: v1.0  
**목적**: 자동화 스크립트 실행을 위한 완벽한 사전 준비 가이드

---

## 📋 목차

1. [개요](#개요)
2. [필수 소프트웨어 설치](#필수-소프트웨어-설치)
3. [계정 준비](#계정-준비)
4. [사전 체크리스트](#사전-체크리스트)
5. [자동화 스크립트 다운로드](#자동화-스크립트-다운로드)
6. [단계별 실행 가이드](#단계별-실행-가이드)
7. [문제 해결](#문제-해결)

---

## 개요

### 자동화 스크립트 구성

이 프로젝트는 **4개의 자동화 스크립트**로 구성되어 있습니다:

| 스크립트 | 목적 | 실행 시점 | 소요 시간 |
|---------|------|----------|-----------|
| `1-setup-dev-environment.sh` | 개발 환경 초기 설정 | 최초 1회 | 5분 |
| `2-auto-deploy-firebase.sh` | Firebase 자동 배포 설정 | 최초 1회 | 10분 |
| `3-dev-workflow.sh` | 일상 개발 워크플로우 | 코드 수정 후 | 2분 |
| `4-master-automation.sh` | 마스터 통합 스크립트 | 언제든지 | 선택 |

### 워크플로우 흐름도

```
┌─────────────────────────────────────────────────┐
│ 최초 설정 (1회만)                               │
├─────────────────────────────────────────────────┤
│ 1. 소프트웨어 설치 (Node.js, Git)              │
│ 2. 계정 준비 (GitHub, Firebase)                │
│ 3. 스크립트 1 실행: 개발 환경 설정              │
│ 4. 스크립트 2 실행: Firebase 자동 배포 설정     │
└─────────────────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────┐
│ 일상 개발 (반복)                                │
├─────────────────────────────────────────────────┤
│ 1. 코드 수정                                    │
│ 2. 스크립트 3 실행: 개발 워크플로우             │
│    → 로컬 테스트 → 커밋 → Push → 자동 배포     │
└─────────────────────────────────────────────────┘
```

---

## 필수 소프트웨어 설치

### 1. Node.js 설치 ⭐ **필수**

#### Windows

1. **다운로드**
   - https://nodejs.org 접속
   - **LTS 버전** (권장: v18.x 이상) 다운로드
   - 설치 파일 실행

2. **설치 확인**
   ```bash
   # 명령 프롬프트 또는 PowerShell에서 실행
   node -v
   npm -v
   ```
   
   **예상 출력:**
   ```
   v18.17.0
   9.8.1
   ```

#### macOS

1. **Homebrew 사용 (권장)**
   ```bash
   # Homebrew 설치 (미설치 시)
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   
   # Node.js 설치
   brew install node
   ```

2. **또는 공식 사이트에서 다운로드**
   - https://nodejs.org
   - macOS Installer (.pkg) 다운로드

3. **설치 확인**
   ```bash
   node -v
   npm -v
   ```

#### Linux (Ubuntu/Debian)

```bash
# NodeSource 저장소 추가
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -

# Node.js 설치
sudo apt-get install -y nodejs

# 설치 확인
node -v
npm -v
```

---

### 2. Git 설치 ⭐ **필수**

#### Windows

1. **다운로드 및 설치**
   - https://git-scm.com/download/win
   - Git for Windows 설치 파일 다운로드
   - 설치 시 **Git Bash 포함** 옵션 선택

2. **설치 확인**
   ```bash
   git --version
   ```
   
   **예상 출력:**
   ```
   git version 2.42.0
   ```

3. **Git 사용자 설정**
   ```bash
   git config --global user.name "Your Name"
   git config --global user.email "your.email@example.com"
   ```

#### macOS

1. **Homebrew 사용 (권장)**
   ```bash
   brew install git
   ```

2. **또는 Xcode Command Line Tools**
   ```bash
   xcode-select --install
   ```

3. **설치 확인 및 설정**
   ```bash
   git --version
   git config --global user.name "Your Name"
   git config --global user.email "your.email@example.com"
   ```

#### Linux (Ubuntu/Debian)

```bash
# Git 설치
sudo apt-get update
sudo apt-get install -y git

# 설치 확인
git --version

# 사용자 설정
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

---

### 3. Python 3 (선택사항)

로컬 테스트 서버 실행 시 사용 (Node.js만으로도 가능)

#### Windows

- https://www.python.org/downloads/
- Python 3.10 이상 다운로드
- 설치 시 **"Add Python to PATH"** 체크

#### macOS

```bash
# Homebrew 사용
brew install python3
```

#### Linux

```bash
# Ubuntu/Debian
sudo apt-get install -y python3

# 확인
python3 --version
```

---

## 계정 준비

### 1. GitHub 계정 ⭐ **필수**

#### 신규 가입 (미가입 시)

1. **가입**
   - https://github.com/join 접속
   - 이메일, 비밀번호, 사용자명 입력
   - 이메일 인증 완료

2. **SSH 키 설정 (권장)**
   ```bash
   # SSH 키 생성
   ssh-keygen -t ed25519 -C "your.email@example.com"
   
   # 공개 키 복사 (Windows)
   clip < ~/.ssh/id_ed25519.pub
   
   # 공개 키 복사 (macOS)
   pbcopy < ~/.ssh/id_ed25519.pub
   
   # 공개 키 복사 (Linux)
   cat ~/.ssh/id_ed25519.pub
   ```
   
3. **GitHub에 SSH 키 등록**
   - https://github.com/settings/keys
   - "New SSH key" 클릭
   - 복사한 키 붙여넣기

#### 저장소 접근 권한

**이 프로젝트의 저장소:**
- https://github.com/jbebakPark/samsung-gfc-recuritment

**권한 요청 방법:**
- 저장소 소유자에게 연락 (jb2park@naver.com)
- Collaborator 또는 팀 멤버로 초대 요청

---

### 2. Firebase 계정 ⭐ **필수**

#### 신규 가입

1. **가입**
   - https://firebase.google.com 접속
   - Google 계정으로 로그인
   - "Get Started" 클릭

2. **프로젝트 접근 권한**
   - 프로젝트명: **samsung-gfc**
   - 프로젝트 ID: **samsung-gfc**
   - 소유자에게 권한 요청 (jb2park@naver.com)

3. **Firebase CLI 설치**
   ```bash
   # npm을 통해 전역 설치
   npm install -g firebase-tools
   
   # 설치 확인
   firebase --version
   ```

4. **Firebase 로그인**
   ```bash
   # 로그인 (브라우저 열림)
   firebase login
   
   # 프로젝트 목록 확인
   firebase projects:list
   ```
   
   **예상 출력:**
   ```
   ┌────────────────┬─────────────┬─────────────────┐
   │ Project ID     │ Display Name│ Resources       │
   ├────────────────┼─────────────┼─────────────────┤
   │ samsung-gfc    │ Samsung GFC │ Hosting, Firestore │
   └────────────────┴─────────────┴─────────────────┘
   ```

---

## 사전 체크리스트

### ✅ 소프트웨어 설치 확인

실행 환경에서 다음 명령어를 실행하여 확인:

```bash
# Node.js
node -v
# 예상: v18.17.0 이상

# npm
npm -v
# 예상: 9.8.1 이상

# Git
git --version
# 예상: 2.40.0 이상

# Firebase CLI
firebase --version
# 예상: 12.0.0 이상

# Python (선택)
python3 --version
# 예상: 3.10.0 이상 (선택사항)
```

**모든 명령어가 버전을 출력하면 ✅**

---

### ✅ 계정 로그인 확인

```bash
# Git 사용자 확인
git config --global user.name
git config --global user.email

# Firebase 로그인 확인
firebase projects:list

# GitHub 접근 확인 (저장소 클론 테스트)
git clone https://github.com/jbebakPark/samsung-gfc-recuritment.git test-repo
cd test-repo
rm -rf ../test-repo  # 테스트 완료 후 삭제
```

**모든 명령어가 오류 없이 실행되면 ✅**

---

### ✅ 네트워크 연결 확인

```bash
# GitHub 연결 테스트
curl https://github.com

# Firebase 연결 테스트
curl https://firebase.google.com

# npm 레지스트리 테스트
npm ping
```

**모든 연결이 성공하면 ✅**

---

## 자동화 스크립트 다운로드

### 방법 1: Git Clone (권장)

```bash
# 저장소 클론
git clone https://github.com/jbebakPark/samsung-gfc-recuritment.git

# 디렉토리 이동
cd samsung-gfc-recuritment

# 스크립트 확인
ls -la scripts/

# 예상 출력:
# 1-setup-dev-environment.sh
# 2-auto-deploy-firebase.sh
# 3-dev-workflow.sh
# 4-master-automation.sh
```

### 방법 2: 수동 다운로드

1. GitHub 저장소 접속
   - https://github.com/jbebakPark/samsung-gfc-recuritment

2. **Code** 버튼 클릭 → **Download ZIP**

3. 압축 해제 후 `scripts/` 디렉토리 확인

---

### 실행 권한 부여

**Linux/macOS:**
```bash
chmod +x scripts/*.sh
```

**Windows (Git Bash):**
```bash
chmod +x scripts/*.sh
```

**실행 권한 확인:**
```bash
ls -la scripts/

# 예상 출력 (실행 권한 있음):
# -rwxr-xr-x  1 user user  2872 Feb 12 07:07 1-setup-dev-environment.sh
```

---

## 단계별 실행 가이드

### 🎯 Phase 1: 최초 설정 (1회만)

#### Step 1: 개발 환경 설정 (5분)

```bash
# 프로젝트 루트 디렉토리에서 실행
./scripts/1-setup-dev-environment.sh
```

**이 스크립트가 하는 일:**
1. ✅ Node.js 설치 확인
2. ✅ Git 설치 확인
3. ✅ Git 저장소 확인 및 Pull
4. ✅ npm 패키지 설치
5. ✅ Firebase CLI 설치
6. ✅ Firebase 로그인 확인
7. ✅ 디렉토리 구조 확인
8. ✅ 환경 정보 출력

**예상 출력:**
```
🚀 삼성생명 GFC 개발 환경 설정
==================================

[1/8] Node.js 설치 확인...
✅ Node.js v18.17.0 설치됨

[2/8] Git 설치 확인...
✅ git version 2.42.0 설치됨

[3/8] 저장소 확인...
✅ Git 저장소 확인됨

[4/8] npm 패키지 설치...
✅ 패키지 설치 완료

[5/8] Firebase CLI 설치...
✅ Firebase CLI 12.5.0 이미 설치됨

[6/8] Firebase 로그인 확인...
✅ Firebase 로그인됨

[7/8] 디렉토리 구조 확인...
✅ public
✅ public/css
✅ public/js
✅ public/admin
✅ scripts

[8/8] 환경 정보
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Node.js: v18.17.0
npm: 9.8.1
Git: 2.42.0
Firebase CLI: 12.5.0
작업 디렉토리: /home/user/webapp
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎉 개발 환경 설정 완료!
```

**⚠️ 문제 발생 시:**
- Firebase 로그인 안 됨 → `firebase login` 실행
- npm 설치 오류 → `npm cache clean --force` 후 재시도

---

#### Step 2: Firebase 자동 배포 설정 (10분)

```bash
./scripts/2-auto-deploy-firebase.sh
```

**이 스크립트가 하는 일:**
1. ✅ Git 저장소 확인
2. ✅ Firebase CLI 확인
3. ✅ Firebase 로그인 확인
4. ✅ CI 토큰 생성 (선택)
5. ✅ GitHub Actions 워크플로우 파일 생성
6. ✅ Git 커밋 및 Push

**중요 단계: CI 토큰 생성**

스크립트가 다음 메시지를 표시하면:
```
CI 토큰을 생성하시겠습니까? (y/N): y
```

**"y" 입력 후 절차:**

1. **브라우저가 열림** → Firebase 로그인
2. **토큰이 터미널에 표시됨** (예시):
   ```
   1//0gFv_abcdefghijklmnopqrstuvwxyz1234567890
   ```
3. **토큰을 복사** (Ctrl+C 또는 마우스 드래그)
4. **GitHub Secrets에 등록:**
   - https://github.com/jbebakPark/samsung-gfc-recuritment/settings/secrets/actions
   - "New repository secret" 클릭
   - Name: `FIREBASE_TOKEN`
   - Value: (복사한 토큰 붙여넣기)
   - "Add secret" 클릭

5. **터미널로 돌아와 Enter 누름**

**예상 출력:**
```
🔥 Firebase 자동 배포 설정
================================

[1/6] Git 저장소 확인...
✅ Git 저장소 확인됨

[2/6] Firebase CLI 확인...
✅ Firebase CLI 준비됨

[3/6] Firebase 로그인 확인...
✅ 로그인 완료

[4/6] CI 토큰 생성...
🔑 CI 토큰 생성 중...
✅ 토큰이 생성되었습니다

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔔 GitHub Secrets 등록 필요!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[5/6] GitHub Actions 워크플로우 파일 생성...
✅ 워크플로우 파일 생성 완료

[6/6] Git 커밋 및 Push...
✅ 커밋 완료
지금 Push하시겠습니까? (Y/n): Y
✅ Push 완료

🎉 Firebase 자동 배포 설정 완료!
```

---

### 🎯 Phase 2: 일상 개발 워크플로우

#### Step 3: 코드 수정 후 배포 (2분)

**시나리오: 파일을 수정하고 배포하고 싶을 때**

```bash
# 1. 파일 수정 (예: VS Code에서 index.html 수정)
code public/index.html

# 2. 워크플로우 스크립트 실행
./scripts/3-dev-workflow.sh
```

**스크립트 진행 과정:**

1. **변경 파일 확인**
   ```
   변경된 파일:
   M  public/index.html
   ```

2. **로컬 테스트 (선택)**
   ```
   로컬 서버를 실행하시겠습니까? (y/N): y
   http://localhost:8000 에서 테스트 중...
   # 브라우저에서 확인 후 Ctrl+C로 종료
   ```

3. **커밋 유형 선택**
   ```
   커밋 유형 선택:
   1) feat      - 새로운 기능 추가
   2) fix       - 버그 수정
   3) docs      - 문서 수정
   ...
   유형 선택 (1-10): 2
   ```

4. **커밋 메시지 입력**
   ```
   커밋 메시지 (간단명료하게): 메인 페이지 오타 수정
   
   커밋 메시지: fix: 메인 페이지 오타 수정
   이대로 커밋하시겠습니까? (Y/n): Y
   ✅ 커밋 완료
   ```

5. **Push 및 자동 배포**
   ```
   Push하시겠습니까? (Y/n): Y
   🚀 Push 시작...
   ✅ Push 완료
   
   🎉 워크플로우 완료!
   🚀 자동 배포: 시작됨 (2-3분 소요)
   
   확인 링크:
   - 배포 상태: https://github.com/jbebakPark/samsung-gfc-recuritment/actions
   - 프로덕션: https://samsung-gfc.web.app
   ```

---

### 🎯 마스터 스크립트 사용

**모든 기능을 하나의 메뉴로:**

```bash
./scripts/4-master-automation.sh
```

**메뉴 화면:**
```
╔════════════════════════════════════════╗
║  삼성생명 GFC 마스터 자동화 도구     ║
╚════════════════════════════════════════╝

▶ 초기 설정 (최초 1회)
  1) 개발 환경 설정
  2) Firebase 자동 배포 설정

▶ 일상 작업
  3) 개발 워크플로우 (코드 수정 → 배포)
  4) 로컬 서버 시작
  5) Firebase 수동 배포

▶ 정보 및 관리
  6) Git 상태 확인
  7) 전체 시스템 점검
  8) 최적화 보고서 보기
  9) 프로젝트 문서 열기

▶ 고급 작업
 10) 전체 자동화 실행 (초기 설정부터 배포까지)
 11) Firebase Functions 배포
 12) 데이터베이스 백업

  0) 종료

선택하세요 (0-12):
```

**사용 예시:**
- 처음 설정: `1` → `2` 순서대로 실행
- 일상 개발: `3` 실행
- 상태 확인: `6` 또는 `7` 실행

---

## 문제 해결

### 1. Node.js 설치 오류

**증상:**
```bash
node: command not found
```

**해결:**
```bash
# Windows: PATH 환경 변수 확인
echo %PATH%

# macOS/Linux: PATH 환경 변수 확인
echo $PATH

# Node.js 재설치
# https://nodejs.org에서 최신 LTS 버전 다운로드
```

---

### 2. Git 인증 오류

**증상:**
```bash
Permission denied (publickey)
```

**해결:**
```bash
# SSH 키 생성
ssh-keygen -t ed25519 -C "your.email@example.com"

# 공개 키 확인
cat ~/.ssh/id_ed25519.pub

# GitHub에 SSH 키 등록
# https://github.com/settings/keys
```

**또는 HTTPS 사용:**
```bash
# Git credential helper 설정
git config --global credential.helper store

# 다음 push 시 비밀번호 입력하면 저장됨
git push
```

---

### 3. Firebase 로그인 오류

**증상:**
```bash
Firebase login failed
```

**해결:**
```bash
# 기존 로그인 제거
firebase logout

# 재로그인
firebase login --reauth

# 프로젝트 목록 확인
firebase projects:list
```

---

### 4. 권한 오류 (Permission Denied)

**증상:**
```bash
./scripts/1-setup-dev-environment.sh: Permission denied
```

**해결:**
```bash
# 실행 권한 부여
chmod +x scripts/*.sh

# 확인
ls -la scripts/
```

---

### 5. npm 설치 오류

**증상:**
```bash
npm ERR! code EACCES
```

**해결:**
```bash
# npm 캐시 정리
npm cache clean --force

# 권한 문제 해결 (Linux/macOS)
sudo chown -R $(whoami) ~/.npm

# 재설치
npm install
```

---

### 6. 방화벽/프록시 오류

**증상:**
```bash
Error: connect ETIMEDOUT
```

**해결:**
```bash
# npm 프록시 설정 (필요 시)
npm config set proxy http://proxy.company.com:8080
npm config set https-proxy http://proxy.company.com:8080

# Git 프록시 설정 (필요 시)
git config --global http.proxy http://proxy.company.com:8080
git config --global https.proxy http://proxy.company.com:8080
```

---

## ✅ 최종 체크리스트

### 설치 완료 확인

- [ ] Node.js 설치 확인 (`node -v`)
- [ ] npm 설치 확인 (`npm -v`)
- [ ] Git 설치 확인 (`git --version`)
- [ ] Firebase CLI 설치 확인 (`firebase --version`)
- [ ] Git 사용자 설정 완료
- [ ] Firebase 로그인 완료

### 계정 준비 확인

- [ ] GitHub 계정 생성 및 로그인
- [ ] 저장소 접근 권한 확보
- [ ] Firebase 계정 생성 및 로그인
- [ ] Firebase 프로젝트 접근 권한 확보

### 스크립트 준비 확인

- [ ] 저장소 클론 완료
- [ ] 스크립트 파일 존재 확인 (`ls scripts/`)
- [ ] 실행 권한 부여 (`chmod +x scripts/*.sh`)

### 초기 설정 완료 확인

- [ ] 스크립트 1 실행 완료 (개발 환경)
- [ ] 스크립트 2 실행 완료 (자동 배포 설정)
- [ ] GitHub Secrets에 FIREBASE_TOKEN 등록
- [ ] GitHub Actions 워크플로우 정상 동작 확인

---

## 📞 지원 및 문의

**문제가 계속 발생하면 연락하세요:**

- **이메일**: jb2park@naver.com
- **전화**: 010-5137-2327
- **카카오톡**: https://open.kakao.com/o/svmDyNUg

**GitHub Issues:**
- https://github.com/jbebakPark/samsung-gfc-recuritment/issues

---

**작성자**: GenSpark AI Developer  
**최종 업데이트**: 2026년 2월 12일  
**다음 문서**: [NEXT_STEPS_DETAILED.md](./NEXT_STEPS_DETAILED.md)

---

_이 가이드를 따라 하면 5-10분 안에 완전 자동화된 개발 환경을 구축할 수 있습니다!_
