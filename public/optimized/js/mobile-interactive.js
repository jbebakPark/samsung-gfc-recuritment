(function() { 'use strict'; console.log('Samsung Mobile Interactive - Loading...'); function initFAQ() { const faqItems = document.querySelectorAll('.faq-item'); if (faqItems.length === 0) { console.log('No FAQ items found'); return; } faqItems.forEach(item => { const question = item.querySelector('.faq-question, .faq-toggle'); if (question) { question.addEventListener('click', function(e) { e.preventDefault(); const isActive = item.classList.contains('active'); if (isActive) { item.classList.remove('active'); } else { item.classList.add('active'); } console.log('FAQ toggled:', item); }); } }); console.log('FAQ initialized:', faqItems.length, 'items'); } function initApplyButtons() { const applyButtons = document.querySelectorAll('.btn-register, .btn-apply, .register-btn, .apply-btn'); if (applyButtons.length === 0) { console.log('No apply buttons found'); return; } applyButtons.forEach(button => { button.addEventListener('click', function(e) { if (this.hasAttribute('href')) { return; } e.preventDefault(); const buttonText = this.textContent.trim(); console.log('Apply button clicked:', buttonText); alert(`신청 기능을 준비 중입니다.\n\n연락처:\n📞 010-5137-2327\n💬 카카오톡: https: }); }); console.log('Apply buttons initialized:', applyButtons.length); } function initCheckboxes() { const checkboxItems = document.querySelectorAll('.checklist-item, .requirement-item'); if (checkboxItems.length === 0) { console.log('No checkbox items found'); return; } checkboxItems.forEach(item => { const checkbox = item.querySelector('input[type="checkbox"]'); const label = item.querySelector('label'); if (checkbox) { checkbox.addEventListener('change', function() { if (this.checked) { item.classList.add('checked'); } else { item.classList.remove('checked'); } }); if (checkbox.checked) { item.classList.add('checked'); } if (label) { item.addEventListener('click', function(e) { if (e.target !== checkbox) { checkbox.checked = !checkbox.checked; checkbox.dispatchEvent(new Event('change')); } }); } } }); console.log('Checkboxes initialized:', checkboxItems.length); } function initModals() { const modalCloseButtons = document.querySelectorAll('.modal-close, .popup-close'); const modalOverlays = document.querySelectorAll('.modal-overlay, .popup-overlay'); modalCloseButtons.forEach(button => { button.addEventListener('click', function(e) { e.preventDefault(); const modal = this.closest('.modal-overlay, .popup-overlay'); if (modal) { modal.style.display = 'none'; document.body.style.overflow = ''; } }); }); modalOverlays.forEach(overlay => { overlay.addEventListener('click', function(e) { if (e.target === this) { this.style.display = 'none'; document.body.style.overflow = ''; } }); }); console.log('Modals initialized'); } function initCardAnimations() { const cards = document.querySelectorAll('.card, .job-fair-card, .event-card, .fair-card'); if (cards.length === 0) { console.log('No cards found for animation'); return; } const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.style.opacity = '1'; entry.target.style.transform = 'translateY(0)'; } }); }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' }); cards.forEach(card => { card.style.opacity = '0'; card.style.transform = 'translateY(20px)'; card.style.transition = 'opacity 0.5s ease, transform 0.5s ease'; observer.observe(card); }); console.log('Card animations initialized:', cards.length); } function initScrollToTop() { const scrollTopButtons = document.querySelectorAll('.scroll-to-top, .back-to-top'); scrollTopButtons.forEach(button => { button.addEventListener('click', function(e) { e.preventDefault(); window.scrollTo({ top: 0, behavior: 'smooth' }); }); }); console.log('Scroll to top initialized'); } function initPhoneFormatting() { const phoneLinks = document.querySelectorAll('a[href^="tel:"]'); phoneLinks.forEach(link => { link.addEventListener('touchstart', function() { this.style.opacity = '0.7'; }); link.addEventListener('touchend', function() { setTimeout(() => { this.style.opacity = '1'; }, 150); }); }); console.log('Phone formatting initialized'); } function initExternalLinks() { const externalLinks = document.querySelectorAll('a[target="_blank"]'); externalLinks.forEach(link => { link.addEventListener('click', function(e) { this.style.opacity = '0.7'; setTimeout(() => { this.style.opacity = '1'; }, 150); }); }); console.log('External links initialized'); } function initCopyToClipboard() { const copyButtons = document.querySelectorAll('[data-copy]'); copyButtons.forEach(button => { button.addEventListener('click', function(e) { e.preventDefault(); const textToCopy = this.getAttribute('data-copy'); if (navigator.clipboard && textToCopy) { navigator.clipboard.writeText(textToCopy).then(() => { const originalText = this.textContent; this.textContent = '복사됨!'; this.style.background = '#48BB78'; setTimeout(() => { this.textContent = originalText; this.style.background = ''; }, 2000); }); } }); }); console.log('Copy to clipboard initialized'); } function initLoadingAnimations() { document.body.style.opacity = '0'; document.body.style.transition = 'opacity 0.3s ease'; window.addEventListener('load', function() { setTimeout(() => { document.body.style.opacity = '1'; }, 100); }); console.log('Loading animations initialized'); } function initLazyLoading() { const images = document.querySelectorAll('img[data-src]'); if (images.length === 0) { return; } const imageObserver = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { const img = entry.target; img.src = img.dataset.src; img.removeAttribute('data-src'); imageObserver.unobserve(img); } }); }); images.forEach(img => imageObserver.observe(img)); console.log('Lazy loading initialized:', images.length, 'images'); } function initTouchRipple() { const rippleElements = document.querySelectorAll('.btn, .card, .faq-question'); rippleElements.forEach(element => { element.addEventListener('touchstart', function(e) { const ripple = document.createElement('span'); ripple.classList.add('ripple'); const rect = this.getBoundingClientRect(); const size = Math.max(rect.width, rect.height); const x = e.touches[0].clientX - rect.left - size / 2; const y = e.touches[0].clientY - rect.top - size / 2; ripple.style.width = ripple.style.height = size + 'px'; ripple.style.left = x + 'px'; ripple.style.top = y + 'px'; this.appendChild(ripple); setTimeout(() => { ripple.remove(); }, 600); }); }); console.log('Touch ripple initialized'); } function initPerformanceMonitoring() { if (window.performance && window.performance.timing) { window.addEventListener('load', function() { const timing = window.performance.timing; const loadTime = timing.loadEventEnd - timing.navigationStart; console.log('Page load time:', (loadTime / 1000).toFixed(2) + 's'); }); } } function init() { if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initializeFeatures); } else { initializeFeatures(); } } function initializeFeatures() { try { initFAQ(); initApplyButtons(); initCheckboxes(); initModals(); initCardAnimations(); initScrollToTop(); initPhoneFormatting(); initExternalLinks(); initCopyToClipboard(); initLoadingAnimations(); initLazyLoading(); initTouchRipple(); initPerformanceMonitoring(); console.log('Samsung Mobile Interactive - All features loaded successfully! ✅'); } catch (error) { console.error('Error initializing features:', error); } } init(); })(); 